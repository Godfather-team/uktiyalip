[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npx -y pnpm@10.26.1 install --frozen-lockfile --filter @workspace/discord-bot..."
]

[start]
cmd = "npx -y pnpm@10.26.1 --filter @workspace/discord-bot start"
